{
  "name": "isimple",
  "private": true,
  "scripts": {
    "dev": "concurrently --names 'frontend,backend' --prefix-colors 'cyan,green' 'pnpm --dir frontend dev' 'cd backend && uv run uvicorn src.immosimple_agent.main:app --reload --port 8000'",
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:backend": "cd backend && uv run uvicorn src.immosimple_agent.main:app --reload --port 8000",
    "build": "pnpm --dir frontend build",
    "lint": "pnpm --dir frontend lint",
    "typecheck": "pnpm --dir frontend typecheck",
    "test": "pnpm lint && pnpm typecheck && pnpm build && pnpm test:backend",
    "test:backend": "cd backend && uv run ruff check src",
    "smoke": "node scripts/smoke.mjs",
    "seed": "pnpm --dir frontend seed"
  },
  "devDependencies": {
    "concurrently": "^9"
  }
}
